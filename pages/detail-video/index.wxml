<!--pages/detail-video/index.wxml-->
<wxs src="../../utils/format.wxs" module="format"></wxs>
<video class="video" 
    src="{{mvURLInfo.url}}"
    autoplay
    referrer-policy="origin"
    loop
    ></video>
 <view class="baseinfo">
   <view class="title">{{mvDetail.name}}</view>
   <view class="singerrc">{{mvDetail.artistName}}</view>
   <view class="count">{{format.formatCount(mvDetail.playCount)}}次数 - {{mvDetail.publishTime}}</view>
 </view>

 <!-- <view class="recommend">
  <view class="title">推荐视频</view>
  <view class="video-list">
    <block wx:for="{{relatedMV}}" wx:key="vid">
      <video-list-item2 itemInfo="{{item}}"></video-list-item2>
    </block>
  </view>
</view> -->